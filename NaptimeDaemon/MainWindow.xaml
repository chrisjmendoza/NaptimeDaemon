<Window x:Class="NaptimeDaemon.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NaptimeDaemon" Height="260" Width="380"
        Icon="Assets/AppIcon.ico">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#F5F5F5"
                CornerRadius="10"
                Padding="12">
            <StackPanel>
                <TextBlock Text="Sleep In" FontSize="12" Opacity="0.75" />
                <TextBlock x:Name="SleepTimerText"
                           FontSize="34"
                           FontWeight="SemiBold"
                           Margin="0,2,0,0" />
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="1"
                   x:Name="IdleThresholdText"
                   Margin="0,10,0,0"
                   FontSize="13"
                   Opacity="0.85" />

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Sleep Now"
                    Height="40"
                    Margin="0,0,10,0"
                    Click="SleepNow_Click" />

            <Button Grid.Column="1"
                    Content="Edit Config"
                    Height="40"
                    Click="EditConfig_Click" />
        </Grid>
    </Grid>
</Window>
